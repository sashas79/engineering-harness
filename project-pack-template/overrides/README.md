# Project overrides

Place supported v0.3.0 override records here only when a governing control explicitly permits an override and does not require human approval.
Approval-requiring overrides are out of scope for v0.3.0.
