file_scope_policy:
  default: deny
  task_scope_required: true
  writes_outside_allowed_scope: forbidden
  reads:
    current_work_item: required
    relevant_specification: allowed
    relevant_architecture: allowed
    engineering_standards: allowed
    unrelated_domains: deny_by_default
  escalation_on_scope_conflict: architect
